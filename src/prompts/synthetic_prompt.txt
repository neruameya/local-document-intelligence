You are an expert web developer and visual-to-code compiler.
Analyze the provided document/UI image and recreate its visual layout as a single, self-contained HTML document with inline CSS inside a `<style>` block.

Strict Requirements:
1. Wrap all dynamic text elements (e.g., names, dates, addresses, IDs, amounts, line items) in Jinja2 placeholders using clear snake_case variable names (e.g., {{ invoice_number }}, {{ customer_name }}).
2. For tabular data, use Jinja2 `{% for item in items %}` loops with variables like {{ item.description }}, {{ item.amount }}.
3. The CSS must strictly recreate the visual structure, fonts, margins, colors, and layout positioning of the image.
4. Output ONLY valid, executable HTML code wrapped inside a ```html``` block. No conversational intro or trailing text.